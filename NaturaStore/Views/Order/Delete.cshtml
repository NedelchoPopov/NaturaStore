@using NaturaStore.Web.ViewModels.Order
@model OrderDeleteViewModel

<h2>Потвърдете изтриването на поръчка</h2>

<div>
    <h4>Поръчка № @Model.Id</h4>
    <p>Дата на създаване: @Model.CreatedOn</p>
    <p>Статус: @Model.Status</p>
</div>

<p>Сигурни ли сте, че искате да изтриете тази поръчка?</p>

<form asp-action="DeleteConfirmed" method="post">
    <input type="hidden" asp-for="Id" />
    <button type="submit" class="btn btn-danger">Изтрий поръчката</button>
    <a href="@Url.Action("Index")" class="btn btn-secondary">Отказ</a>
</form>
